# Phase 0 Baseline Visuals

This directory freezes the current visual-baseline inventory for the viewer-platform refactor.

It intentionally does not duplicate PNG binaries.

- the committed goldens already live in `apps/web/e2e/visual-regression.spec.ts-snapshots/`
- the active visual evidence note is `docs/qa/phase5-visual-regression-2026-04-22.md`
- the structured inventory for Phase 0 is `manifest.json` in this folder

Use this folder as the source of truth for what visual evidence existed before the new `viewer-core` cutover starts.
