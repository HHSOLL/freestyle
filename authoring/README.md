# Authoring Workspace

This directory is the offline asset workspace for the mannequin-first fitting pipeline.

It is not served directly by the web app.

Use it for:

- avatar source files
- garment Blender source files
- export manifests
- validation notes

Only optimized and approved runtime assets should be promoted into `apps/web/public/assets/**`.
