# Cloth 4A QA Evidence (2026-04-07)

## Verdict
- `PASS`

## Scope
- 대상: preview-grade cloth 4A spike acceptance
- 범위: 선택 레이어 1개(`tops|outerwear`) cloth 경로 + fallback 안전성 + 통합 품질 게이트

## Environment
- commit base: `4615cf0` (validation 시작 시점)
- OS/CPU: `Darwin 25.4.0 arm64`
- Node: `v25.2.1`
- npm: `11.6.2`

## Command Evidence
1. Contracts/intake gate
- `npm run validate:garment3d`
- 결과: `Garment 3D catalog validation passed for 2 entries.`

2. Repo quality gate
- `npm run check`
- 결과: `lint/typecheck/build:services/build` 통과
- 참고: 기존 warning 1건(`MannequinScene3D.tsx`의 `<img>` 권고)만 존재, error 없음

3. Core regression
- `npm run test:core`
- 결과: `15 passed, 0 failed`

4. Cloth spike benchmark
- `node scripts/bench-cloth-spike.mjs`
- 결과(JSON):
  - `fpsEquivalent: 8155.97` (solver-step CPU benchmark 기준, run마다 변동 가능)
  - `resetCount: 45`
  - `failureCount: 0`
  - `guard.lowFpsTrip: true`
  - `guard.invalidDeltaTrip: true`
  - `pass: true`

5. Route smoke (local dev)
- `npm --prefix apps/web run dev -- --port 4100`
- `curl -I http://127.0.0.1:4100/studio`
- `curl -I http://127.0.0.1:4100/app/closet`
- `curl -I http://127.0.0.1:4100/app/closet/item/demo`
- 결과: 모두 `HTTP/1.1 200 OK`

## Acceptance Mapping
1. 평균 성능 기준
- 브라우저 실측 FPS trace 대신 solver-step benchmark로 최소 성능 안전선 확인 (`pass=true`).

2. 슬라이더 급변/복구성
- benchmark에서 주기적 body perturbation + reset(`resetCount: 45`) 수행 시 실패 카운트 0.

3. fallback 복귀
- 코드 경로상 low-fps/invalid-delta failure guard가 자동 fallback 트리거하도록 유지됨.
- benchmark의 guard 시나리오(`lowFpsTrip`, `invalidDeltaTrip`) true로 확인.

4. 통합 안정성
- `check`, `test:core`, `validate:garment3d`, route smoke 통과.

## Notes
- 본 evidence는 로컬/헤드리스 중심 검증이다.
- 브라우저별 GPU/드라이버 편차 검증은 운영 canary 단계에서 추가 관찰 대상으로 유지한다.
